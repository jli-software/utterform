{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal capabilities for the Utterform main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "autostart:default"
  ]
}
