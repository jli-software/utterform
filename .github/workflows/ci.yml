name: CI

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}

jobs:
  test:
    name: Linux checks (no release packaging)
    runs-on: ubuntu-22.04
    env:
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"

    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
          # whisper.cpp's build takes its instruction set from a CMake
          # toolchain file that `cmake-rs` never reports to Cargo, so a cached
          # `whisper-rs-sys` is reused even after that file changes. The suffix
          # is what retires such a build; raise it whenever the toolchain file
          # does. `scripts/test-linux-system-package.sh` and the local Whisper
          # test catch a stale one regardless.
          key: linux-x86_64-portable-cpu-1

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang cmake dbus libasound2-dev libayatana-appindicator3-dev librsvg2-dev libssl-dev libwebkit2gtk-4.1-dev

      - run: npm ci
      - run: npm run check
      - run: npm test
      - run: npm run build
      - name: Test production GUI
        if: runner.os == 'Linux'
        run: |
          npx playwright install --with-deps chromium
          npx playwright test
      - uses: actions/upload-artifact@v4
        if: always() && runner.os == 'Linux'
        with:
          name: ui-test-results
          path: test-results/
          retention-days: 14

      - run: cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
      - run: cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
      # A private session bus lets the StatusNotifierItem tray test
      # register and receive a real Activate call.
      - run: dbus-run-session -- cargo test --manifest-path src-tauri/Cargo.toml
